const anonima = function () {
  console.log("Uma função anônima.");
};

anonima();

const somar = function (a, b) {
  return `SOMA: ${a} + ${b} = ${a + b}`;
};

console.log(somar(102, 35));

console.log("");
