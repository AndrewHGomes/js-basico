const anonymaFunc = function () {
  console.log("Uma função anônima.");
};

anonymaFunc();

const soma = function (a, b) {
  return `SOMA: ${a} + ${b} = ${a + b}`;
};

console.log(soma(102, 35));

console.log("");
